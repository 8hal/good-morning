# History 화면 개선 작업 지시서

> 마지막 업데이트: 2026-02-16

## 1. 현재 상태

### 1.1. 있는 것
- `lib/screens/history/history_screen.dart`: 세션 목록 (날짜 + 출퇴근 + 시간만 표시)
- `lib/providers/history_provider.dart`: `FutureProvider<List<Session>>` (완료 세션만 조회)
- `FirestoreService.getSessionHistory()`: 페이지네이션 지원 세션 목록 조회
- `FirestoreService.getBlocks(sessionId)`: 세션의 블록 목록 조회
- 라우팅: `/history` (하단 탭 2번째)
- `Session` 모델: `overallSatisfaction`, `energyAtStart` 필드 존재
- `Block` 모델: `timeFeel`, `satisfaction`, `actualMinutes`, `endedBy` 필드 존재

### 1.2. 없는 것
- 세션 상세 화면 (`/history/:sessionId`)
- 주간/월간 통계 화면
- 목록에서 상세로 탐색하는 네비게이션
- 블록별 실행 결과 표시
- 날짜별 그룹핑

---

## 2. 목표

### Phase 1: 목록 + 상세 (이 지시서의 범위)
세션 목록을 보기 좋게 정리하고, 탭하면 세션 상세 화면으로 진입하여 블록별 실행 기록을 확인할 수 있다.

### Phase 2: 통계 (향후)
주간/월간 통계. 이 지시서 범위 밖.

---

## 3. 화면 설계

### 3.1. History 목록 화면 (기존 개선)

**기존**: 단순 리스트, 정보 부족
**개선**: 날짜별 그룹핑 + 카드 형태

```
[📅 기록]

── 오늘 ──────────────────────────
┌─────────────────────────────────┐
│ 🏠 재택                         │
│ 06:30 → 09:45  (3시간 15분)     │
│ ✓ 5개 블록 완료                  │
│                    [상세보기 >]  │
└─────────────────────────────────┘

── 어제 ──────────────────────────
┌─────────────────────────────────┐
│ 🏢 출근                         │
│ 07:00 → 10:20  (3시간 20분)     │
│ ✓ 6개 블록 완료                  │
│                    [상세보기 >]  │
└─────────────────────────────────┘

── 2월 14일 금요일 ───────────────
┌─────────────────────────────────┐
│ 🏢 출근                         │
│ 06:45 → 09:30  (2시간 45분)     │
│ ✓ 4개 블록 완료                  │
│                    [상세보기 >]  │
└─────────────────────────────────┘
```

**표시 정보**:
- 출퇴근 타입 아이콘 + 라벨
- 시작 → 종료 시간 (소요 시간)
- 완료 블록 수

**기능**:
- Pull-to-refresh (기존 유지)
- 카드 탭 → 상세 화면 이동
- 빈 상태, 에러 상태 (기존 유지)

### 3.2. History 상세 화면 (신규)

**경로**: `/history/:sessionId`

```
[← 뒤로]  [2월 16일 일요일]

┌─ 세션 요약 ─────────────────────┐
│ 🏠 재택                         │
│ 06:30 → 09:45                   │
│ 소요: 3시간 15분                 │
│ 만족도: ⭐⭐⭐⭐☆ (4/5)         │
│ 시작 에너지: 😊 좋음             │
└─────────────────────────────────┘

┌─ 블록 타임라인 ─────────────────┐
│                                  │
│ 06:30  ● 명상 ──── 15분         │
│            😊 적당 · ⭐4         │
│                                  │
│ 06:45  ● 운동 ──── 30분         │
│            😰 길었다 · ⭐3       │
│                                  │
│ 07:15  ● 샤워 ──── 25분 (⚡-5분)│
│            😊 적당 · ⭐5         │
│                                  │
│ 07:40  ● 아침 식사 ── 30분      │
│            😊 적당 · ⭐4         │
│                                  │
│ 08:10  ● 뉴스 확인 ── 20분      │
│            😰 길었다 · ⭐3       │
│                                  │
│ 09:45  ● 세션 종료               │
└─────────────────────────────────┘
```

**표시 정보**:
- 세션 요약 카드:
  - 출퇴근 타입
  - 시작 → 종료 시간
  - 총 소요 시간
  - `overallSatisfaction` (있는 경우)
  - `energyAtStart` (있는 경우)
- 블록 타임라인:
  - 각 블록: 시작 시간, 이름(`displayLabel`), 실제 소요 시간
  - 예상 대비 차이: `actualMinutes - plannedMinutes` (±표시)
  - `timeFeel` (있는 경우): 짧았다/적당/길었다
  - `satisfaction` (있는 경우): 별점 표시
  - 피드백 없는 블록은 시간만 표시

---

## 4. 파일 변경 사항

### 4.1. 수정할 파일

| 파일 | 변경 내용 |
|------|----------|
| `lib/screens/history/history_screen.dart` | 카드 형태 UI로 전면 개선 |
| `lib/providers/history_provider.dart` | 블록 수 포함한 데이터 조회 추가 |
| `lib/router/app_router.dart` | `/history/:sessionId` 라우트 추가 |
| `lib/core/utils/datetime_utils.dart` | 날짜 그룹핑 유틸 추가 (오늘/어제/날짜) |

### 4.2. 새로 만들 파일

| 파일 | 내용 |
|------|------|
| `lib/screens/history/session_detail_screen.dart` | 세션 상세 화면 |
| `lib/screens/history/widgets/session_card.dart` | 목록의 세션 카드 위젯 |
| `lib/screens/history/widgets/block_timeline.dart` | 상세 화면의 블록 타임라인 위젯 |
| `lib/providers/session_detail_provider.dart` | 세션 + 블록 데이터 조회 |

---

## 5. 구현 명세

### 5.1. `datetime_utils.dart` 추가 메서드

```dart
/// 날짜를 그룹 라벨로 변환 ("오늘", "어제", "2월 14일 금요일")
static String formatDateGroup(DateTime dt) {
  final now = DateTime.now();
  final today = DateTime(now.year, now.month, now.day);
  final dateOnly = DateTime(dt.year, dt.month, dt.day);
  final diff = today.difference(dateOnly).inDays;

  if (diff == 0) return '오늘';
  if (diff == 1) return '어제';
  return DateFormat('M월 d일 EEEE', 'ko_KR').format(dt);
}

/// 소요 시간 포맷 ("3시간 15분", "45분")
static String formatDuration(Duration duration) {
  final hours = duration.inHours;
  final minutes = duration.inMinutes % 60;
  if (hours > 0) return '$hours시간 $minutes분';
  return '$minutes분';
}
```

### 5.2. `session_detail_provider.dart`

```dart
/// 세션 상세 데이터 (세션 + 블록 목록)
class SessionDetail {
  final Session session;
  final List<Block> blocks;
  const SessionDetail({required this.session, required this.blocks});
}

final sessionDetailProvider =
    FutureProvider.family<SessionDetail, String>((ref, sessionId) async {
  final firestoreService = ref.watch(firestoreServiceProvider);
  
  // 세션 문서 직접 조회 (FirestoreService에 메서드 추가 필요)
  final session = await firestoreService.getSession(sessionId);
  final blocks = await firestoreService.getBlocks(sessionId);
  
  return SessionDetail(session: session, blocks: blocks);
});
```

### 5.3. `FirestoreService` 추가 메서드

```dart
/// 세션 단건 조회 (ID 기반)
Future<Session> getSession(String sessionId) async {
  final doc = await _db
      .collection(AppConstants.sessionsCollection)
      .doc(sessionId)
      .get();
  return Session.fromFirestore(doc);
}
```

### 5.4. `app_router.dart` 라우트 추가

```dart
// ShellRoute 밖에 추가 (하단 탭 없이 full-screen)
GoRoute(
  path: '/history/:sessionId',
  name: 'sessionDetail',
  builder: (context, state) {
    final sessionId = state.pathParameters['sessionId']!;
    return SessionDetailScreen(sessionId: sessionId);
  },
),
```

### 5.5. History 목록 화면 개선 포인트

```dart
// 날짜별 그룹핑 로직
Map<String, List<Session>> groupByDate(List<Session> sessions) {
  final grouped = <String, List<Session>>{};
  for (final s in sessions) {
    final key = DateTimeUtils.formatDateGroup(s.startAt);
    grouped.putIfAbsent(key, () => []).add(s);
  }
  return grouped;
}
```

- `ListView` 대신 `CustomScrollView` + `SliverList` 사용 (날짜 헤더 + 카드)
- 각 세션 카드에 블록 수를 표시하려면: `historyProvider` 개선 필요
  - **옵션 A**: 세션에 `blockCount` 필드를 추가하여 세션 종료 시 저장 (권장 - 추가 쿼리 없음)
  - **옵션 B**: 목록 조회 시 각 세션별 블록 count 쿼리 (N+1 문제)
  - **옵션 C**: 블록 수 표시 생략, 상세에서만 확인 (간단)
  - → **옵션 C를 권장**. 블록 수는 상세 화면에서 확인. 목록은 시간 정보만.

### 5.6. 블록 타임라인 위젯

```dart
class BlockTimeline extends StatelessWidget {
  final List<Block> blocks;

  // 각 블록을 타임라인 형태로 표시
  // - 왼쪽: 시작 시간
  // - 중앙: 점 + 연결선
  // - 오른쪽: 블록 정보 카드
  //   - displayLabel, 소요 시간
  //   - timeFeel 아이콘 (있는 경우)
  //   - satisfaction 별점 (있는 경우)
  //   - 예상 대비 차이 (actualMinutes vs plannedMinutes)
}
```

**timeFeel 아이콘 매핑**:
- `short_` → "⚡ 짧았다"
- `ok` → "👍 적당"
- `long_` → "🐌 길었다"

**예상 대비 차이 표시**:
- `actualMinutes < plannedMinutes` → "⚡ -5분" (초록색)
- `actualMinutes > plannedMinutes` → "⏰ +10분" (주황색)
- `actualMinutes == plannedMinutes` → 표시 안 함

---

## 6. 주의사항

### 6.1. 실험 중립성
- ~~점수 평균, 분석, 추천~~ → 보여주지 않음
- 과거 기록은 **사실 기반 정보만** 표시 (시간, 블록, 피드백)
- `overallSatisfaction`과 `satisfaction`은 사용자 본인이 입력한 값이므로 표시 가능

### 6.2. 데이터 없는 경우 처리
- `overallSatisfaction == null` → 만족도 섹션 숨김
- `energyAtStart == null` → 에너지 섹션 숨김
- `timeFeel == null && satisfaction == null` → 피드백 라인 숨김
- 블록 0개 → "기록된 블록이 없습니다" 메시지

### 6.3. 성능
- 목록 화면: 기존 `getSessionHistory()` 사용 (페이지네이션 유지)
- 상세 화면: `getSession()` + `getBlocks()` 2개 쿼리만 (가벼움)
- 목록에서 블록 수 쿼리 하지 않음 (N+1 방지)

### 6.4. 디자인 가이드
- Material 3 컴포넌트 사용
- 앱 전체 테마 따르기 (다크/라이트 자동)
- 카드: `Card` + `Padding`, 과도한 그림자 금지
- 타임라인: 커스텀 위젯 (세로선 + 점) 또는 `Stepper` 활용 가능

---

## 7. 작업 순서 (권장)

| 단계 | 작업 | 예상 시간 |
|------|------|----------|
| 1 | `DateTimeUtils`에 유틸 메서드 추가 | 5분 |
| 2 | `FirestoreService.getSession()` 추가 | 5분 |
| 3 | `session_detail_provider.dart` 생성 | 10분 |
| 4 | `session_card.dart` 위젯 생성 | 15분 |
| 5 | `history_screen.dart` 개선 (그룹핑 + 카드) | 20분 |
| 6 | `block_timeline.dart` 위젯 생성 | 20분 |
| 7 | `session_detail_screen.dart` 생성 | 20분 |
| 8 | `app_router.dart` 라우트 추가 | 5분 |
| 9 | `flutter analyze` + `flutter build web` 검증 | 5분 |
| 10 | Firebase Hosting 배포 | 3분 |
| **합계** | | **~108분** |

---

## 8. 검증 체크리스트

- [ ] History 탭에서 세션 목록이 날짜별로 그룹핑되어 표시됨
- [ ] 세션 카드에 출퇴근 타입, 시간, 소요 시간이 정확히 표시됨
- [ ] 카드 탭 시 상세 화면으로 정상 이동
- [ ] 상세 화면에서 세션 요약 정보가 표시됨
- [ ] 상세 화면에서 블록 타임라인이 시간순으로 표시됨
- [ ] 피드백 있는 블록은 timeFeel + satisfaction 표시
- [ ] 피드백 없는 블록은 시간 정보만 표시
- [ ] 데이터 없는 경우 빈 상태 화면 정상 표시
- [ ] 에러 발생 시 에러 UI + 재시도 버튼 정상 동작
- [ ] Pull-to-refresh 정상 동작
- [ ] 뒤로가기 정상 동작 (상세 → 목록)
- [ ] `flutter analyze` 에러 없음
- [ ] `flutter build web` 성공
